<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=Satisfy&display=swap"
      rel="stylesheet"
    />

    <title>Login Chatterbox</title>
    <style>
      .font-satisfy {
        font-family: "Satisfy", cursive;
      }
      .font-press-start {
        font-family: "Press Start 2P", cursive;
      }
    </style>
  </head>

  <body class="flex items-center justify-center min-h-screen bg-black p-4">
    <div
      id="login"
      class="w-full max-w-md p-6 bg-gray-900 rounded-[15px] shadow-[1px_1px_20px_1px_rgb(200,200,200)] transition-all duration-300 hover:scale-105"
    >
    
      <div class="p-6 text-center">
        <h1 class="text-2xl text-white font-press-start mb-2">
          Chatterbox
        </h1>
        <p class="text-gray-300 text-sm font-satisfy">
          Allows users to create a unique profile.
        </p>
      </div>

      <h2 class="mb-4 text-2xl font-semibold text-center text-white">
        User Login
      </h2>

      <form action="/users/login" method="post" class="space-y-4">
        <div>
          <input
            type="email"
            id="email"
            name="email"
            placeholder="Enter your email"
            required
            class="w-full p-3 mt-1 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 bg-black text-white placeholder-gray-400"
          />
        </div>
        <div>
          <input
            type="password"
            id="password"
            name="password"
            placeholder="Enter your password"
            required
            class="w-full p-3 mt-1 border rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500 bg-black text-white placeholder-gray-400"
          />
        </div>
        <button
          type="submit"
          class="w-full p-3 text-white bg-blue-600 rounded-md hover:bg-blue-700 font-semibold"
        >
          Login Now
        </button>
      </form>

      <p class="mt-4 text-center text-sm text-gray-300">
        Don't have an account?
        <a href="/users/register" class="text-blue-500 hover:underline font-semibold">
          Register here
        </a>
      </p>

      <!-- KEEPING YOUR GOOGLE LOGIN BUTTON STYLE -->
      <a
        href="/auth/google/"
        class="flex items-center w-[50%] justify-center mx-auto space-x-2 p-2 border rounded-lg shadow mt-4"
      >
        <img
          src="https://www.svgrepo.com/show/475656/google-color.svg"
          alt="Google Logo"
          class="w-6 h-6"
        />
        <span class="text-white hover:text-blue-500">Login with Google</span>
      </a>
    </div>
    <script>
      window.addEventListener("load", () => {
        const form = document.querySelector("form");
        if (form) {
          form.reset(); // clears all input fields on page load
        }
      });
    </script>
    
  </body>
</html>
